From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: OpenClaw Agent <agent@openclaw.ai>
Date: Wed, 6 May 2026 19:20:00 +0800
Subject: [PATCH] uapi: add include guards to ioctl macro definitions

Add #ifndef guards to ioctl macros in asm-generic/ioctl.h to prevent
macro redefinition warnings when userspace (musl/glibc) also defines
these macros (~177 warnings).

musl's bits/ioctl.h defines _IOC, _IO, _IOR, _IOW, _IOWR with its own
bit layout. When both kernel UAPI headers and musl headers are in the
include path, every macro is redefined. By adding #ifndef guards, the
kernel definitions yield to whichever was included first.

Signed-off-by: OpenClaw Agent <agent@openclaw.ai>
---
 include/uapi/asm-generic/ioctl.h | 58 +++++++++++++++++++++++++-------
 1 file changed, 46 insertions(+), 12 deletions(-)

--- a/include/uapi/asm-generic/ioctl.h
+++ b/include/uapi/asm-generic/ioctl.h
@@ -20,8 +20,12 @@
  * this explicit here.  Please be sure to use the decoding macros
  * below from now on.
  */
+#ifndef _IOC_NRBITS
 #define _IOC_NRBITS	8
+#endif
+#ifndef _IOC_TYPEBITS
 #define _IOC_TYPEBITS	8
+#endif
 
 /*
  * Let any architecture override either of the following before
@@ -36,15 +40,31 @@
 # define _IOC_DIRBITS	2
 #endif
 
+#ifndef _IOC_NRMASK
 #define _IOC_NRMASK	((1 << _IOC_NRBITS)-1)
+#endif
+#ifndef _IOC_TYPEMASK
 #define _IOC_TYPEMASK	((1 << _IOC_TYPEBITS)-1)
+#endif
+#ifndef _IOC_SIZEMASK
 #define _IOC_SIZEMASK	((1 << _IOC_SIZEBITS)-1)
+#endif
+#ifndef _IOC_DIRMASK
 #define _IOC_DIRMASK	((1 << _IOC_DIRBITS)-1)
+#endif
 
+#ifndef _IOC_NRSHIFT
 #define _IOC_NRSHIFT	0
+#endif
+#ifndef _IOC_TYPESHIFT
 #define _IOC_TYPESHIFT	(_IOC_NRSHIFT+_IOC_NRBITS)
+#endif
+#ifndef _IOC_SIZESHIFT
 #define _IOC_SIZESHIFT	(_IOC_TYPESHIFT+_IOC_TYPEBITS)
+#endif
+#ifndef _IOC_DIRSHIFT
 #define _IOC_DIRSHIFT	(_IOC_SIZESHIFT+_IOC_SIZEBITS)
+#endif
 
 /*
  * Direction bits, which any architecture can choose to override
@@ -66,11 +86,13 @@
 # define _IOC_READ	2U
 #endif
 
+#ifndef _IOC
 #define _IOC(dir,type,nr,size) \
 	(((dir)  << _IOC_DIRSHIFT) | \
 	 ((type) << _IOC_TYPESHIFT) | \
 	 ((nr)   << _IOC_NRSHIFT) | \
 	 ((size) << _IOC_SIZESHIFT))
+#endif
 
 #ifndef __KERNEL__
 #define _IOC_TYPECHECK(t) (sizeof(t))
@@ -82,26 +104,58 @@
  * NOTE: _IOW means userland is writing and kernel is reading. _IOR
  * means userland is reading and kernel is writing.
  */
+#ifndef _IO
 #define _IO(type,nr)		_IOC(_IOC_NONE,(type),(nr),0)
+#endif
+#ifndef _IOR
 #define _IOR(type,nr,size)	_IOC(_IOC_READ,(type),(nr),(_IOC_TYPECHECK(size)))
+#endif
+#ifndef _IOW
 #define _IOW(type,nr,size)	_IOC(_IOC_WRITE,(type),(nr),(_IOC_TYPECHECK(size)))
+#endif
+#ifndef _IOWR
 #define _IOWR(type,nr,size)	_IOC(_IOC_READ|_IOC_WRITE,(type),(nr),(_IOC_TYPECHECK(size)))
+#endif
+#ifndef _IOR_BAD
 #define _IOR_BAD(type,nr,size)	_IOC(_IOC_READ,(type),(nr),sizeof(size))
+#endif
+#ifndef _IOW_BAD
 #define _IOW_BAD(type,nr,size)	_IOC(_IOC_WRITE,(type),(nr),sizeof(size))
+#endif
+#ifndef _IOWR_BAD
 #define _IOWR_BAD(type,nr,size)	_IOC(_IOC_READ|_IOC_WRITE,(type),(nr),sizeof(size))
+#endif
 
 /* used to decode ioctl numbers.. */
+#ifndef _IOC_DIR
 #define _IOC_DIR(nr)		(((nr) >> _IOC_DIRSHIFT) & _IOC_DIRMASK)
+#endif
+#ifndef _IOC_TYPE
 #define _IOC_TYPE(nr)		(((nr) >> _IOC_TYPESHIFT) & _IOC_TYPEMASK)
+#endif
+#ifndef _IOC_NR
 #define _IOC_NR(nr)		(((nr) >> _IOC_NRSHIFT) & _IOC_NRMASK)
+#endif
+#ifndef _IOC_SIZE
 #define _IOC_SIZE(nr)		(((nr) >> _IOC_SIZESHIFT) & _IOC_SIZEMASK)
+#endif
 
 /* ...and for the drivers/sound files... */
 
+#ifndef IOC_IN
 #define IOC_IN		(_IOC_WRITE << _IOC_DIRSHIFT)
+#endif
+#ifndef IOC_OUT
 #define IOC_OUT		(_IOC_READ << _IOC_DIRSHIFT)
+#endif
+#ifndef IOC_INOUT
 #define IOC_INOUT	((_IOC_WRITE|_IOC_READ) << _IOC_DIRSHIFT)
+#endif
+#ifndef IOCSIZE_MASK
 #define IOCSIZE_MASK	(_IOC_SIZEMASK << _IOC_SIZESHIFT)
+#endif
+#ifndef IOCSIZE_SHIFT
 #define IOCSIZE_SHIFT	(_IOC_SIZESHIFT)
+#endif
 
 #endif /* _UAPI_ASM_GENERIC_IOCTL_H */
